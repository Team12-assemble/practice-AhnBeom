const inputValue = document.querySelector(".inputValue");
const btn = document.querySelector("button");

const initValue = inputValue.value;

function multiple() {}

btn.addEventListener("click", (e) => {
  e.preventDefault();
  console.log(btn.value);
});

inputValue.value = result;
